# data (large; download separately — see README)
data/
# python
__pycache__/
*.pyc
# logs / scratch
*.log
# latex build artifacts
*.aux
*.out
*.synctex.gz
report.pdf

# checkpoint cache (850M, regenerable from the run scripts)
runs_v2/ckpt/
