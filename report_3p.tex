\documentclass[10pt,twocolumn]{article}
\usepackage[margin=1.7cm,columnsep=0.55cm]{geometry}
\usepackage{amsmath,amssymb,graphicx,booktabs,microtype}
\usepackage[hidelinks]{hyperref}
\usepackage[small,compact]{titlesec}
\setlength{\parskip}{1pt}
\setlength{\textfloatsep}{9pt}
\setlength{\floatsep}{9pt}
\setlength{\intextsep}{9pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{2pt}

\title{\vspace{-7mm}\large\bf Horizon-Gated JEPA: Label-Free Separation of\\
Slow and Fast Factors in Time Series}
\author{Jaechan Lee (21261919)}
\date{July 24, 2026 --- term report for GENE501}

\begin{document}
\maketitle
\vspace{-9mm}

\section{Motivation and claim}
Practitioners want the \emph{slowly-varying state} of a time series (machine
health, diagnosis, activity) \emph{separated} from fast variation, not merely
co-encoded: separation is what enables selective domain robustness and
interpretable state trajectories. Frequency decomposition
\cite{mallat1999wavelet,huang1998empirical} fails in principle --- a slow
factor need not live in a low band (bearing health rides the envelope of
high-frequency resonance \cite{randall2011rolling}); sequential VAEs
(DSAE \cite{hsu2017unsupervised}, C-DSVAE \cite{bai2021contrastively})
separate only \emph{static} content via fragile reconstruction; and
horizon-conditioned JEPAs (HEPA \cite{hepa2026}) encode multi-scale dynamics
without factoring them.

Our observation: \textbf{the prediction horizon --- free in any predictive
learner --- is itself a supervision axis}, because only slow information
helps predict the far future
\cite{bialek2001predictability,creutzig2009past}. \emph{H1: gating a latent
block out of the predictor at long horizons concentrates slow factors in the
ungated block, with no labels.} Two refinements set the claim's exact
strength. \emph{H2 (assignment asymmetry):} the gate \emph{assigns} slow
information to the ungated block; \emph{exclusion} of fast information rests on
a narrow bottleneck plus a decorrelation penalty. \emph{H3:} the gate requires
\emph{latent} targets; raw-signal prediction smears detail everywhere. Unlike
slow feature analysis \cite{wiskott2002slow} we select by predictive value,
not trajectory slowness; unlike temporal nonlinear-ICA
(TCL \cite{hyvarinen2016unsupervised}, SlowVAE \cite{klindt2021towards}) we
have no full identifiability theorem --- our inductive bias, in the sense
required by \cite{locatello2019challenging}, is the timescale gap itself.

\begin{figure}[t]
\centering
\includegraphics[width=.96\linewidth]{fig_method.pdf}
\vspace{-4mm}
\caption{\small \textbf{Method.} \textbf{A:} the encoder splits each embedding
into a narrow slow block and a wide fast block; the slow path is always open,
the fast path is gated by $g(\Delta)$; dcor decorrelates blocks. \textbf{B:}
the gate closes beyond $\tau=c\,T_{\mathrm{ac}}$, set label-free from the
signal's autocorrelation time.}
\vspace{-3mm}
\end{figure}

\section{Method}
A causal transformer encoder ($\sim$0.5M params) maps patches to
$z_t=[z^{\mathrm{slow}}_t;z^{\mathrm{fast}}_t]\in\mathbb R^{64}$ (16 dims
slow). A predictor conditioned on $\Delta\in\{1,4,16,64,128\}$ patches
regresses the EMA target encoder's embedding (CPC multi-horizon predictor
\cite{oord2018representation} with I-JEPA-style targets
\cite{assran2023self,baevski2022data2vec}, as in HEPA \cite{hepa2026}; our
contribution is the gate, not the backbone):
\[
\mathcal L=\textstyle\sum_{\Delta}\big\|
P\big(z^{\mathrm{slow}}_t, g(\Delta)z^{\mathrm{fast}}_t, \Delta\big)
-\bar z_{t+\Delta}\big\|^2+\lambda\mathcal L_{\mathrm{dcor}},
\]
with soft gate $g(\Delta)=\sigma((\tau-\Delta)/w)$, where $\sigma$ is the
logistic sigmoid and $w$ (4 patches) sets the transition width, so the fast
block fades smoothly from open ($g{\approx}1$) to closed ($g{\approx}0$)
around $\tau$. \textbf{Why asymmetric.} The design mirrors an asymmetry in the
generative process. (i) Fast dynamics are \emph{conditioned on} the slow
state --- the next gait sample needs the current activity, the next
oscillation sample needs the regime's carrier --- so short-horizon targets
need both blocks. (ii) The fast factor forgets itself within its autocorrelation
time $T_{\mathrm{ac}}$, so the far future needs only the slow block. A gate should
therefore remove only the information that has become useless: closing the
fast path at long horizons is harmless. The symmetric variant, closing the
slow path at short horizons, would instead cut information the short-horizon
loss still needs, forcing the encoder to copy slow information into
$z^{\mathrm{fast}}$ --- the very duplication separation must prevent.
$\mathcal L_{\mathrm{dcor}}$ penalizes squared cross-covariance
(VICReg-lineage \cite{bardes2022vicreg}). The threshold is $\tau=c\,T_{\mathrm{ac}}$, where $T_{\mathrm{ac}}$ is the
\emph{autocorrelation time} --- the lag at which the signal's autocorrelation
decays to $1/e$, i.e.\ how long the fast dynamics stay correlated before
forgetting themselves. We set $c\!\approx\!2.5$ so that $\tau$ sits a few
autocorrelation times out, past which the fast factor has essentially decayed
and only slow information remains predictive. $T_{\mathrm{ac}}$ is read
directly from the data, so $\tau$ requires no labels.

\section{Two claims about what the gate can and cannot do}
We state the mechanism as two claims; the argument is intuitive and we keep it
informal here.

\textbf{Claim 1 (inclusion --- slow information lands in $z^{\mathrm{slow}}$).}
Suppose the far future depends on the past only through the current slow state
(the timescale gap), and the gate is closed at some long horizon, so there the
predictor sees only $z^{\mathrm{slow}}$. To predict that far future the encoder
then has no choice but to pack everything relevant into $z^{\mathrm{slow}}$ ---
in particular, enough to tell which slow state the series is in. So the gate
\emph{forces} the slow factor into the ungated block; inclusion is automatic.
(One mild condition: different states must leave a distinguishable imprint on
the predicted embedding; states that differ only in higher-order statistics,
not in the average, are a corner case we flag as open.)

\textbf{Claim 2 (no exclusion --- the loss cannot push fast information out).}
The training loss cares only about prediction quality, never about which block
holds what. So one can always reshuffle $z^{\mathrm{slow}}$ reversibly, leaving
the loss unchanged, in a way that smuggles fast information back in --- nothing
in the objective forbids it, at any gate setting, and the decorrelation penalty
only erases \emph{linear} traces. This is why a little fast information still
leaks (the nonlinear-probe residual in F4): it is a property of the objective,
not a bug. What actually keeps fast information out in practice is the
combination of a narrow slow block and the smoothness of a real network, which
cannot implement those adversarial reshufflings --- an empirical effect, not a
guarantee.

\begin{figure}[t]
\centering
\includegraphics[width=.9\linewidth]{fig_mech.pdf}
\vspace{-4mm}
\caption{\small \textbf{Mechanism (synthetic, group/time-split, 3 seeds).}
\textbf{A:} $z^{\mathrm{slow}}$ probe scores --- blue is slow kept (higher
better), yellow/green are fast leak (lower better). Only gate+dcor separates;
swapping the JEPA loss for a contrastive latent one (CPC, F2) does slightly
better; the raw-target (AR) baseline fails. \textbf{B:}
block$\times$factor score matrices. \textbf{C:} fast leak vs.\ width --- a
fixed 16-dim block holds at $d_z{=}256$ while a proportionally scaled block
leaks.}
\vspace{-3mm}
\end{figure}

\section{Experiments}
\textbf{Datasets (Tab.~1).} Each dataset supplies a labeled \emph{slow}
factor and a measurable \emph{fast} factor, used to evaluate separation
(slow kept, fast leaked). \emph{Training} never sees labels; labels enter only
at evaluation, and --- as a limitation we return to --- in choosing
$d_{\mathrm{slow}}$ and $\lambda$.

\emph{Synthetic two-timescale oscillator} --- the mechanism testbed, with
recorded ground truth for both factors. The observation is a single
oscillating signal $x_t=\sin\varphi_t+0.2\,u_t+\text{noise}$. The
\textbf{slow factor} is a hidden 3-state regime $s_t$ (Markov chain, mean
dwell 3000 steps) whose \emph{only} effect is setting the carrier frequency
($\pm5\%$). The \textbf{fast factor} is an OU process $u_t$ (memory
$\approx$50 steps) that jitters the instantaneous frequency by a comparable
amount, plus the oscillation phase itself. Amplitudes are identical across
regimes and the regime's frequency shift is within the jitter range, so
\emph{no short-window statistic reveals the regime} --- identifying it
requires integrating hundreds of steps, exactly the long-horizon information
the gate is meant to isolate.

\emph{HAPT} \cite{reyes2016transition} --- smartphone IMU: the quantity of
interest (activity, persisting $\sim$12\,s) is buried under $\sim$1\,Hz gait
oscillation (\textbf{slow} $=$ activity, \textbf{fast} $=$ acceleration
magnitude).
\emph{PTB-XL} \cite{wagner2020ptb} --- 12-lead ECG (\textbf{slow} $=$
normal-vs-abnormal ``NORM'' diagnosis, binary macro-F1; \textbf{fast} $=$
beat waveform). An acknowledged \emph{limiting} case: our method assumes the
slow factor \emph{varies slowly}, so the gate has a live-vs-forgotten
timescale contrast to exploit. Here the diagnosis does not vary at all over a
10\,s record --- it is \emph{static}, the very content-vs-dynamics regime that
sequential VAEs target and that we argue is too narrow. With no slow variation
to isolate we expect the gate to help little, so we treat PTB-XL as a stress
test at the edge of our assumptions, not a showcase.
\emph{XJTU-SY} \cite{wang2020xjtu} --- bearing vibration, included as the
\emph{applicability boundary}: intended slow $=$ health, fast $=$ carrier,
but both live at one timescale within a window, so the method predicts
non-separation (F5).

\textbf{Protocol.} To check what a block encodes we fit a simple probe on top
of it and read its score. The probe is trained on one set of groups and
tested on entirely different, held-out groups (Tab.~1: subjects for HAPT,
patients for PTB-XL, bearings for XJTU). This avoids the standard time-series
leak, where overlapping windows put near-identical snippets on both sides of
the split and inflate the score; here no window overlaps and no group is
shared. A useful side effect: because the test groups are unseen, HAPT and
PTB-XL also measure generalization to new subjects/patients (domain shift),
the selective robustness we motivate. We report \emph{absolute} scores
(accuracy / $R^2$ / macro-F1), with RankMe \cite{garrido2023rankme} monitoring
collapse so a low leak cannot be an artifact of an empty block. $n{=}3$ seeds, one backbone and budget for all
variants; one patch is 8 steps, so horizons $\{1{,}4{,}16{,}64{,}128\}$ span
$8$--$1024$ steps. Results are findings F1--F5.

\begin{table}[t]
\centering\small
\caption{\small Datasets: factor assignment, split, and role.}
\begin{tabular}{@{}lllll@{}}
\toprule
Dataset & slow & fast & held out & role \\
\midrule
Synthetic & regime $s$ & OU $u$, phase & future seg. & mechanism \\
HAPT & activity & acc.\ magn. & subjects & robustness \\
PTB-XL & diagnosis & beat value & patients & static (limit) \\
XJTU-SY & health & carrier & bearings & neg.\ control \\
\bottomrule
\end{tabular}
\end{table}

\textbf{F1 --- separation, and alternatives do not suffice (Fig.~2A, Tab.~2).}
Gate+dcor keeps the regime in $z^{\mathrm{slow}}$ while cutting fast leak
roughly fourfold. It even reads the regime slightly better than the full
embedding: dropping fast dimensions removes noise for the slow task. Neither
shortcut substitutes. Label-free rotations of the ungated embedding
(SFA/ICA/PCA) each trade one side for the other (Tab.~2), so separation is
created by the objective, not recoverable post hoc. A theory-bearing
competitor, an architecture-matched SlowVAE \cite{klindt2021towards}, recovers
the regime only weakly --- its reconstruction is local, so a factor needing
integration over hundreds of steps barely enters its code.

\begin{table}[h]
\centering\small
\caption{\small Unmixing control (synthetic, 3 seeds): 16-dim ``slow''
subspace of the ungated embedding vs.\ our gate.}
\begin{tabular}{@{}lccc@{}}
\toprule
Method & regime $\uparrow$ & $u$-leak $\downarrow$ & phase-leak $\downarrow$\\
\midrule
SFA (16 slowest) & 0.57 & 0.00 & 0.00 \\
ICA (16 slowest) & 0.52 & 0.12 & 0.17 \\
PCA (top 16)     & 0.76 & 0.80 & 0.91 \\
\textbf{gate+dcor (ours)} & \textbf{0.80} & \textbf{0.17} & \textbf{0.28}\\
\bottomrule
\end{tabular}
\end{table}

\textbf{F2 --- latent targets, either objective (Fig.~2A).} The identical gate
on raw-patch (AR) targets separates nothing: raw targets force fast detail
into every dimension. Our main backbone uses the JEPA regression loss, but to
test whether the effect is specific to it we also swap in a latent
\emph{contrastive} objective (CPC-InfoNCE, which scores the true future
embedding against in-batch negatives instead of regressing it). It separates
at least as well --- in fact slightly better here, with $z^{\mathrm{fast}}$
still holding the fast factors (no collapse). We read this as evidence that
the mechanism needs a \emph{latent} target rather than the JEPA loss
specifically; why contrastive edges ahead is an open observation, not yet a
settled conclusion, so we keep JEPA as the default backbone.
\textbf{F3 --- gate assigns, penalty purifies (Fig.~2A; Claim~2).} Gate-alone
barely reduces leak. dcor-alone degenerates, satisfying decorrelation by
emptying the slow block. Only the combination separates: the gate gives each
block a job, the penalty removes duplicates. Adding target-side removal
pressure does not rescue gate-alone, matching Claim~2.
\textbf{F4 --- exclusion rides on capacity, and scales (Fig.~2C).} Leak tracks
the \emph{absolute} slow-block size. A fixed 16-dim block keeps its exclusion
when the backbone widens to $d_z{=}256$, whereas a proportionally scaled block
lets leak flood back. So the mechanism transfers to wide backbones, provided
the slow block stays narrow.
\textbf{F5 --- real data (Fig.~3, Tab.~3).} On HAPT the gate cuts fast leak
sharply while keeping activity F1, and this holds on \emph{unseen subjects}
--- the selective robustness the method is meant to give. On PTB-XL the effect
is smaller and needs dcor; whether dcor helps or hurts is data-dependent
(its \emph{polarity} flips between datasets) and not yet decidable without
labels. XJTU is the predicted negative: carrier and health share one timescale
within a window, so the gate correctly does nothing. There the non-circular
health target (lifetime fraction, held-out bearings) is hard for every method,
and the learned $z^{\mathrm{slow}}$ carries what little exists. The classical
envelope and low-pass features do worse, but that is a repurposing failure,
not a verdict on classical practice: envelope analysis is built for
fault-frequency \emph{detection}, not cross-bearing regression. We read XJTU
only as a correctly-predicted non-separable case.

\begin{table}[h]
\centering\small
\caption{\small Real data, group split, absolute scores (3 seeds). Slow-kept
$=$ $z^{\mathrm{slow}}$ label score; bracket $=$ the ungated full-embedding
score (an upper reference).}
\begin{tabular}{@{}llcc@{}}
\toprule
Dataset & Variant & slow-kept $\uparrow$ & fast-leak $\downarrow$\\
\midrule
HAPT (act F1) & gate & \textbf{0.71} [0.80] & \textbf{0.17}\\
              & gate+dcor & 0.67 & 0.28\\
              & no gate & 0.62 & 0.56\\
PTB-XL (norm F1) & gate+dcor & 0.70 [0.74] & \textbf{0.41}\\
                 & gate & 0.72 & 0.52\\
                 & no gate & 0.71 & 0.61\\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=.92\linewidth]{fig_real.pdf}
\vspace{-4mm}
\caption{\small \textbf{Real data (3 seeds).} Gate and gate+dcor on both
datasets: dcor hurts HAPT (\textbf{A}), helps PTB-XL (\textbf{B}).
\textbf{C:} XJTU, the predicted negative. Bars are $R^2$ on the health
target, which is negative when a predictor does worse than the mean; every
method is weak and the classical features fail to transfer across bearings.}
\vspace{-3mm}
\end{figure}

\section{Limitations and future work}
\textbf{Limitations.} Exclusion is soft and \emph{linear-level}: nonlinear
probes recover more of the fast factor than linear ones (MLP $R^2$ $0.45$
vs.\ $0.19$), which is exactly the boundary Claim~2 identifies --- our claim
is linear-subspace separation, not information-theoretic removal. Most importantly, on real data the demonstrated benefit is so far the
\emph{internal} separation metric (lower leak); the payoffs the introduction
motivates (robustness under a fast-dynamics shift, interpretable
trajectories, attribution) are not yet measured there, and gated training in
fact has a downstream cost (HAPT: full embedding $0.80\!\to\!0.76$;
$z^{\mathrm{slow}}$ alone $0.71$). The synthetic win of $z^{\mathrm{slow}}$
over the full embedding ($0.80$ vs.\ $0.74$) is in-distribution selective
use, not robustness-under-shift; an explicit shift experiment is the first
priority. The dcor polarity is data-dependent and not yet
decidable label-free; $\tau$ is label-free but
$d_{\mathrm{slow}},\lambda$ are not. The design assumes exactly two
timescales, and $\tau=c\,T_{\mathrm{ac}}$ assumes fast dynamics dominate the
ACF. Training is fully label-free; validation, as standard in
disentanglement, is not.
\textbf{Future work (in progress or planned).} A shift-robustness experiment
(above); unsupervised model selection (a health-metric criterion currently
rejects broken configs but cannot fine-tune); anomaly attribution via
horizon-split residuals (point detection works in a pilot, contextual does
not); more datasets with pre-registered gap screening (Sleep-EDF
\cite{kemp2000analysis}, C-MAPSS \cite{saxena2008damage}); external baselines
(TS2Vec \cite{yue2022ts2vec}, masked reconstruction); a nonlinear (HSIC)
penalty for the linear-level limitation; and understanding why the contrastive
objective separates slightly better than JEPA regression (F2), which may
motivate revisiting the default backbone.

\clearpage
\begin{thebibliography}{99}\setlength{\itemsep}{2pt}
\bibitem{wiskott2002slow} L. Wiskott and T. J. Sejnowski. Slow feature
analysis: unsupervised learning of invariances. \emph{Neural Computation},
14(4):715--770, 2002.
\bibitem{bialek2001predictability} W. Bialek, I. Nemenman, and N. Tishby.
Predictability, complexity, and learning. \emph{Neural Computation},
13(11):2409--2463, 2001.
\bibitem{creutzig2009past} F. Creutzig, A. Globerson, and N. Tishby.
Past-future information bottleneck in dynamical systems. \emph{Physical Review
E}, 79(4):041925, 2009.
\bibitem{hyvarinen2016unsupervised} A. Hyv\"arinen and H. Morioka.
Unsupervised feature extraction by time-contrastive learning and nonlinear
ICA. In \emph{Advances in Neural Information Processing Systems (NeurIPS)},
2016.
\bibitem{klindt2021towards} D. A. Klindt, L. Schott, Y. Sharma, I. Ustyuzhaninov,
W. Brendel, M. Bethge, and D. Paiton. Towards nonlinear disentanglement in
natural data with temporal sparse coding. In \emph{International Conference on
Learning Representations (ICLR)}, 2021.
\bibitem{locatello2019challenging} F. Locatello, S. Bauer, M. Lucic, G.
R\"atsch, S. Gelly, B. Sch\"olkopf, and O. Bachem. Challenging common
assumptions in the unsupervised learning of disentangled representations. In
\emph{International Conference on Machine Learning (ICML)}, 2019.
\bibitem{hsu2017unsupervised} W.-N. Hsu, Y. Zhang, and J. Glass. Unsupervised
learning of disentangled and interpretable representations from sequential
data. In \emph{Advances in Neural Information Processing Systems (NeurIPS)},
2017.
\bibitem{bai2021contrastively} J. Bai, W. Wang, and C. P. Gomes. Contrastively
disentangled sequential variational autoencoder. In \emph{Advances in Neural
Information Processing Systems (NeurIPS)}, 2021.
\bibitem{oord2018representation} A. van den Oord, Y. Li, and O. Vinyals.
Representation learning with contrastive predictive coding.
\emph{arXiv:1807.03748}, 2018.
\bibitem{assran2023self} M. Assran, Q. Duval, I. Misra, P. Bojanowski, P.
Vincent, M. Rabbat, Y. LeCun, and N. Ballas. Self-supervised learning from
images with a joint-embedding predictive architecture (I-JEPA). In
\emph{IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)},
2023.
\bibitem{baevski2022data2vec} A. Baevski, W.-N. Hsu, Q. Xu, A. Babu, J. Gu,
and M. Auli. data2vec: a general framework for self-supervised learning in
speech, vision and language. In \emph{International Conference on Machine
Learning (ICML)}, 2022.
\bibitem{hepa2026} J. Petersen, G.-A. Lombardi, R. Maggioni, C. Mazzoleni, F.
Martelli, and P. Petersen. HEPA: a self-supervised horizon-conditioned event
predictive architecture for time series. \emph{arXiv:2605.11130}, 2026.
\bibitem{bardes2022vicreg} A. Bardes, J. Ponce, and Y. LeCun. VICReg:
variance-invariance-covariance regularization for self-supervised learning. In
\emph{International Conference on Learning Representations (ICLR)}, 2022.
\bibitem{garrido2023rankme} Q. Garrido, R. Balestriero, L. Najman, and Y.
LeCun. RankMe: assessing the downstream performance of pretrained
self-supervised representations by their rank. In \emph{International
Conference on Machine Learning (ICML)}, 2023.
\bibitem{yue2022ts2vec} Z. Yue, Y. Wang, J. Duan, T. Yang, C. Huang, Y. Tong,
and B. Xu. TS2Vec: towards universal representation of time series. In
\emph{AAAI Conference on Artificial Intelligence}, 2022.
\bibitem{mallat1999wavelet} S. Mallat. \emph{A Wavelet Tour of Signal
Processing}. Academic Press, 2nd edition, 1999.
\bibitem{huang1998empirical} N. E. Huang, Z. Shen, S. R. Long, M. C. Wu, H. H.
Shih, Q. Zheng, N.-C. Yen, C. C. Tung, and H. H. Liu. The empirical mode
decomposition and the Hilbert spectrum for nonlinear and non-stationary time
series analysis. \emph{Proceedings of the Royal Society of London A},
454(1971):903--995, 1998.
\bibitem{randall2011rolling} R. B. Randall and J. Antoni. Rolling element
bearing diagnostics --- a tutorial. \emph{Mechanical Systems and Signal
Processing}, 25(2):485--520, 2011.
\bibitem{reyes2016transition} J.-L. Reyes-Ortiz, L. Oneto, A. Sam\`a, X.
Parra, and D. Anguita. Transition-aware human activity recognition using
smartphones. \emph{Neurocomputing}, 171:754--767, 2016.
\bibitem{wagner2020ptb} P. Wagner, N. Strodthoff, R.-D. Bousseljot, D.
Kreiseler, F. I. Lunze, W. Samek, and T. Schaeffter. PTB-XL, a large publicly
available electrocardiography dataset. \emph{Scientific Data}, 7:154, 2020.
\bibitem{wang2020xjtu} B. Wang, Y. Lei, N. Li, and N. Li. A hybrid prognostics
approach for estimating remaining useful life of rolling element bearings
(XJTU-SY datasets). \emph{IEEE Transactions on Reliability}, 69(1):401--412,
2020.
\bibitem{saxena2008damage} A. Saxena, K. Goebel, D. Simon, and N. Eklund.
Damage propagation modeling for aircraft engine run-to-failure simulation
(C-MAPSS). In \emph{International Conference on Prognostics and Health
Management (PHM)}, 2008.
\bibitem{kemp2000analysis} B. Kemp, A. H. Zwinderman, B. Tuk, H. A. C. Kamphuisen,
and J. J. L. Obery\'e. Analysis of a sleep-dependent neuronal feedback loop:
the slow-wave microcontinuity of the EEG (Sleep-EDF). \emph{IEEE Transactions
on Biomedical Engineering}, 47(9):1185--1194, 2000.
\end{thebibliography}

\end{document}
