# CLAUDE_ko.md — HGLP (Horizon-Gated Latent Prediction)

> **영문 `CLAUDE.md`가 공식 기준이고 이 문서는 한국어 미러입니다.**
> 결정이 바뀌면 `CLAUDE.md` → `docs/PROTOCOL.md` → `docs/PROTOCOL_ko.md` 순으로
> 반영하며, 미러에만 반영되는 수정은 금지합니다.
> 마지막 동기화: 2026-08-07

이 파일은 이 저장소의 모든 세션이 읽는 상시 문맥입니다. 무엇이든 손대기 전에
읽으십시오. 프로젝트가 **무엇인지**, 무엇이 **결정되었는지**, 무엇을 **조용히
바꾸면 안 되는지**를 담습니다. 할 일 목록은 `docs/CHECKLIST_v3.md`(실행용)와
`docs/CHECKLIST_v2.md`(참고용)에 있습니다.

---

## 1. 이 프로젝트가 무엇인가 — 다섯 문장 지도

1. **한 문장 아이디어:** 먼 미래를 조금이라도 예측하려면 모델은 현재 상태를
   **적어두어야** 한다. 그 메모지가 `z_slow`다.
2. **한 개의 메커니즘:** 지속성 축 위의 단 하나의 절단 τ. τ보다 오래 기억되는
   것은 전부 `z_slow`로, 더 빨리 잊히는 것은 전부 `z_mix`로.
3. **두 개의 정직성 조항:** 포함(inclusion)은 목적함수가 보장한다(*Gating*).
   배제(exclusion)는 **보장하지 않는다** — 목적함수는 빠른 정보를 `z_slow` 밖으로
   밀어낼 수 없다(*Exclusion*). 실제로는 좁은 블록과 인코더의 매끄러움이
   경험적으로, **선형 수준에서만** 그 일을 한다.
4. **세 개의 적용 조건:** (i) 윈도우를 지배하는 coherent-periodic 성분이 없을 것,
   (ii) 시간척도 간극이 존재할 것(τ 너머까지 지속되는 성분), (iii) 관심 인자가
   학습 코퍼스 안에서 실제로 변할 것. 정상 데이터만으로 하는 이상탐지는
   **명시적으로 범위 밖**이다.
5. **세 개의 사용 프로토콜:** `z_slow`만 쓰는 probe(shift 강건성, 라벨 효율성) ·
   `z_slow` 궤적(건강 지표) · 블록별 질의(`z_mix`의 결함 유형, 점/문맥 잔차).

나머지는 전부 이 다섯 중 하나의 각주입니다.

## 2. 명명 (현행, 필수)

- 프로젝트: **HGLP** (Horizon-Gated Latent Prediction). "JEPA"는 README 부제와
  초록의 검색 키워드로만 쓰고, **방법 이름으로는 절대 쓰지 않습니다.**
- 두 스템 — 백본·게이트·벌점은 같고 손실 층만 다릅니다.
  - **HGLP-Reg** = `loss=l1 target=ema` (EMA 타깃 인코더에 대한 L1;
    붕괴는 EMA 비대칭으로 처리)
  - **HGLP-NCE** = `loss=nce target=ema` (배치 내 음성에 대한 InfoNCE,
    같은 윈도우 음성은 마스킹; 붕괴는 음성 + EMA로 처리)
  - **2026-08-04 개정(사용자 승인), 근거: 4스템 × 3시드 × 3데이터셋 + gate×xcov
    어블레이션.** 두 스템 모두 Part 1과 Part 2에 사용합니다. D4의 "하나만 고르기"는
    실데이터 재실행을 피하려던 것인데 H200에서 전체 매트릭스가 6~9분이라 그
    비용 근거가 소멸했습니다.
    - Reg의 손실 L2 → **L1**: L2는 시드마다 불안정하고(합성 slow-kept
      0.641±0.239, 시드별 0.513/0.975/0.434) 느린 인자를 잃습니다. L1이
      복구합니다(0.980±0.010). 결함은 **L2에 국한**되며, 제곱 오차의 그래디언트가
      큰 잔차에 지배되어 약한 느린 신호를 익사시키기 때문입니다. L1(그래디언트
      크기 일정)과 InfoNCE(순위)는 각각 다른 방식으로 이를 피합니다. HEPA가
      L1을 쓴 이유를 독립적으로 재현한 셈입니다. **L2는 스템이 아니라
      어블레이션으로 유지합니다.**
    - NCE의 타깃 online → **EMA**: online 형태의 낮은 leak은 부분적으로
      공허합니다 — `z_mix`가 빠른 프록시를 HAPT 0.163 / PTB-XL 0.615로만 담는데,
      `nce+ema`는 0.628 / 0.881입니다. 즉 빠른 인자가 `z_slow`에서 배제된 게
      아니라 **어디에도 약하게만 표현된** 것입니다. **online은 어블레이션으로
      유지합니다.** 이는 D2의 타깃 선택을 대체하며, D2의 ⛔ 조건(불안정)은
      한 번도 발동하지 않았습니다 — online은 잘 학습되고, 분리를 못 할 뿐입니다.
- 벌점: **`L_xcov`** (블록 간 교차공분산의 제곱). 옛 이름 `dcor`는 Székely의
  distance correlation과 충돌하므로 **재사용 금지**입니다.
- **블록: `z_slow` / `z_mix` (논문 본문 전용, 2026-08-06 채택).** 여집합 블록을
  산문·그림·표에서 **`z_mix`**라고 부릅니다. **코드와 저장된 모든 JSON 키는
  `z_fast` 그대로 둡니다** — 리네임은 §4-6에 따라 마감 후 작업이므로, 스프린트
  중에는 `probes.BLOCKS`, `block_factor`, run 파일을 건드리지 마십시오.
  이유: 게이트는 그 블록을 Δ < τ에서만 노출하는데, 가까운 horizon 예측은 빠른
  인자만큼이나 **현재의 느린 상태**를 필요로 하므로 그 블록은 **두 인자를 모두**
  담습니다(세 데이터셋에서 느린 인자 실측 0.36~0.87). 내용에는 제약이 없고
  **가용 horizon만** 제약됩니다. "fast"라고 부르면 우리가 주장한 적 없는
  대칭적 분리를 독자가 기대하게 됩니다 — §5의 "분리는 설계상 한 방향" 참조.
- **산문 용어: 지속(persistent) / 일시(transient). slow / fast 아님 (2026-08-07 채택).**
  우리가 가르는 축은 **지속성**(τ 너머까지 예측 가능한가)이지 스펙트럼상의 느림이
  아닙니다. 둘은 mixing 가정 아래에서만 일치하며, 합성 생성기가 그 일치를 의도적으로
  깹니다 — 지속 요인이 캐리어 **주파수**를 정하므로 저역통과로는 복원되지 않습니다.
  이게 중요한 이유는 **Slow Feature Analysis와 비교하기 때문**입니다. "slow factor"라고
  쓰면 독자가 SFA와 같은 일을 다른 알고리즘으로 한다고 읽습니다. 기호는 `z_slow` /
  `z_mix`를 유지하고, 첫 등장에서 "지속 블록" / "나머지"로 한 번 연결합니다.
- 이론 절: **Gating / Exclusion** (비형식적 논증). **"Prop. 1 / Prop. 2"는 금지** —
  정리가 아닙니다.
- 옛 CLI 이름(`nepa`, `dcor=`)은 한 커밋 동안만 deprecated alias로 남깁니다.

## 3. 잠긴 결정 D1–D7 (사용자 승인, 불변)

- **D1** (2026-08-02 개정, 승인) τ 앵커: `estimate_tac`의 `chosen` =
  파생 계열(raw / 제곱에너지 / 포락선) 중 **T_ac 최솟값**을 **u 스케일로 변환**한 값
  (제곱 계열 ×2 — 제곱은 OU 상관시간을 절반으로 만듦, raw·포락선 ×1). 빠른
  수명의 하한 추정이며, 안전한 방향입니다(τ가 너무 짧으면 무게이트 통제 쪽으로
  실패하지 거짓 분리로 가지 않습니다). **τ = c·chosen, c = ln(1/eps),
  eps = 0.05 ⟹ c ≈ 3.0** (eps는 게이트에서 허용하는 잔여 빠른 자기상관).
  `c=`는 부록 스윕 오버라이드로만 남깁니다. 합성 검증 기준: 계열별 에너지
  T_ac ≈ 3.125 패치(±25%), 변환 후 chosen ≈ 5.24, τ ≈ 15.7 — 레거시 하드코딩
  τ=16과 사후적으로 일치합니다(관측된 것이지 튜닝된 것이 아님).
- **D2** NCE online 타깃: **양방향 그래디언트, EMA 없음, stop-grad 없음.**
  불안정하면 ⛔ 중단하고 보고. 자율 폴백 금지.
  **2026-08-04 대체(승인) → NCE 스템은 EMA 타깃을 씁니다.** ⛔는 발동한 적이
  없고(online은 안정적으로 학습됨) 분리를 더 못 할 뿐입니다. online은
  어블레이션으로 유지합니다. 근거는 §2.
- **D3** `make_dataset(..., seed=seed)`: 시드가 데이터를 바꿉니다. 모든 합성
  오차막대를 재생성하며, 재실행은 야간 배치에 포함합니다.
- **D4** Part-2 스템 규칙: slow-kept와 leak 양쪽에서 우세한 스템이 Part 2를
  담당. 지표가 엇갈리면 Reg 유지.
  **2026-08-04 대체(승인) → 두 스템 모두 Part 2를 담당합니다.** D4의 규칙은
  실데이터 재실행을 피하려던 것인데 전체 매트릭스가 6~9분이라 비용 근거가
  소멸했고, §2가 애초에 두 개의 정식 스템을 정의합니다. 둘 다 보고하는 것이
  더 강한 주장이기도 합니다 — 게이트/xcov 메커니즘이 두 손실 계열에서 성립.
- **D5** 절단 순서(승인): NCE d_slow → coherence 스윕 → HI → HSIC → TS2Vec
  (고전 베이스라인이 답해진 경우에만).
- **D6** 근접 산출물: **2026-08-08 23:59 동부시간 — IEEE 8쪽 수업 논문
  (Responsible-AI 과목).** 워크숍 버전은 그 이후에 확장합니다.
  **2026-08-07 사용자가 8/7 → 8/8로 연장.** 사용자 시계는 `America/Toronto`,
  이 장비는 UTC로 4시간 빠릅니다. 시각을 인용하거나 마감까지 남은 시간을
  계산할 때는 반드시 `TZ=America/Toronto date`를 쓰십시오.
- **D7** vfloor 규칙: `vfloor=0`에서 RankMe 붕괴가 나면 그 항이 논문 손실식에
  들어가고, 붕괴가 없으면 기본 off로 두고 제거.
  **2026-08-03 해결 → 제거.** `vfloor=0`에서 붕괴 없음(RankMe가 13.8→22로
  오히려 **상승**). 이 항은 중립적이지도 않았습니다 — 오차막대를 넓히던 s0의
  u-leak 이상치(0.50→−0.09)를 함께 끌고 있었습니다. `vfloor=` 플래그는 모든
  트레이너에서 기본 0이고, vf1 셀은 부록 D7 어블레이션으로 강등됩니다.

## 4. 에이전트를 위한 강제 규칙 (관리자·서브에이전트 공통)

1. **문서 위계:** `docs/CHECKLIST_v3.md`를 실행합니다. `CHECKLIST_v2.md`는
   배경·백로그이며 충돌 시 v3가 이깁니다. v3가 명시하지 않은 v2 항목은
   착수하지 않습니다.
2. **⛔ 중단 조건:** (a) 스크린의 Check 1이 합성 테스트베드를 기각할 때,
   (b) D2 하에서 NCE-online 학습이 불안정할 때. **중단하고 보고하고 대기.**
3. **숫자를 지어내거나 손으로 옮기지 마십시오.** 모든 그림·표의 숫자는
   `runs/*.json`에서 재생성됩니다. 이 논문은 그림↔본문 불일치 사고가
   **두 번** 있었습니다.
4. **옛 `cpc_*` run을 삭제하거나 재실행하지 마십시오** — P0-3 스템 분리 이후
   그것들이 정확히 `nce+ema` 통제 셀입니다.
5. **논문에서의 정직한 보고:** 미완의 작업은 "in progress"로 표시하고,
   뒷받침하는 run 태그가 없는 주장은 삭제하거나 강등합니다.
   **자리표시자 숫자는 절대 금지.**
6. 스프린트 중 저장소 전역 리네임이나 대규모 리팩터 금지(논문 본문은 HGLP
   명명을 쓰되 코드 리네임은 마감 후 백로그).

## 5. 에이전트가 잘못 재유도하면 안 되는 도메인 사실

- **백본 귀속** (2026-08-03 논문 원문 대조 재확인, 이전의 "점 타깃" 표현은
  부정확했고 사용자 승인 하에 여기서 정정):
  - **v1 (옛 코드가 한 것):** 타깃은 causal 인코더의 출력을 t+Δ에서 *읽은* 것 —
    즉 **x[0 … t+Δ]의 누적 요약**이며, 앵커 자신의 과거 x[0…t]를 다시 포함합니다.
    이를 "점 타깃"이라 부른 것은 수용장을 숨긴 표현이었습니다. 정확한 표현:
    *0에서 시작해 t+Δ에서 읽은 causal 누적 요약*.
  - **v2 (결정):** **수용장 한정 점 타깃** —
    `TargetEncoder(x[t+Δ−w_eff : t+Δ])`를 마지막 위치에서 읽으며,
    `w_eff = min(w, Δ)`이므로 타깃의 수용장이 항상 `(t, t+Δ]` 안에 있고 앵커의
    과거를 다시 포함하지 않습니다. 이것이 harmless-to-close를 희망이 아니라
    **구성상** 성립하게 만듭니다.
  - **CPC** [van den Oord 2018]: 타깃은 **국소** 인코더 `g_enc`의 `z_{t+k}`이며,
    누적 문맥 `c_t`는 입력 쪽에만 나타납니다. 따라서 CPC도 앵커의 과거를
    타깃에서 배제합니다.
  - **HEPA**: `(t, t+Δ]`에 대한 구간 요약 타깃(양방향 + 어텐션 풀링), 가중치
    공유·공동 학습 타깃(EMA 없음, stop-grad 없음), SIGReg, L1,
    **사인파 절대 위치**, **스칼라(연속) Δ 조건화** — 즉 연속 horizon 조건화는
    표준 선택이지 우리 기여가 아닙니다.
  - **LeNEPA**: 다음 스텝(t+1) 잠재 타깃, horizon 변수 없음.
  - **"as in HEPA"라고 절대 쓰지 마십시오.** 산문에서는 전체 대비를 Related
    Works에 1~2문장으로 두고, Method는 타깃 형태에 정확히 한 문장만 씁니다.
    살아 있는 차이는 **"미래 구간 안의 한 위치를 읽는다(우리) vs 구간 전체를
    어텐션 풀링한다(HEPA)"**이며, 풀링은 가까운 미래의 빠른 내용을 섞어 들여
    harmless-to-close 논증을 약화시킵니다.
- **왜 먼 horizon 예측이 애초에 작동하는가:** 손실은 조건부 기댓값입니다.
  예측기는 regime 전환을 미리 볼 수 없고, **현재 상태를 인코딩해야만** 오차를
  낮출 수 있습니다. `z_slow`는 **미래에 대한 추측이 아니라 지금에 대한 메모**입니다.
  (리뷰어는 기본적으로 이걸 오독하므로, 논문에 2~3문장 문단을 둡니다.)
- **"느림"은 장기-horizon-예측가능(지속적)의 축약어입니다.** 지속성이 느림과
  일치하는 것은 mixing-fast 가정 아래에서뿐입니다. Coherent periodic 성분은
  느리지 않으면서 무한히 지속되며, 그것이 스크린 Check 1의 표적입니다.
  정적 인자는 무한 지속의 *상수* 극한이고 설계상 `z_slow`로 들어옵니다
  (그것이 기능입니다. PTB-XL의 진단이 예시).
- **적용 영역:** dwell ≈ Δ_max → 느린(+정적) 분리 [HAPT, 합성];
  dwell ≫ Δ_max이지만 시퀀스마다 다름 → 정적 분리 [PTB-XL];
  코퍼스 전체에서 불변 → 범위 밖 [정상 데이터만 있는 설정].
  XJTU-SY = 예측된 음성 사례(시간척도 간극 없음).
- **세 번째 적용 축 — 윈도우 수준 인자 오염 (2026-08-03 추가):**
  dwell 대 Δ_max와는 **별개**입니다. "윈도우가 인자 전환을 가로지르는가?"
  HAPT 전처리는 활동 경계를 무시하고 고정 스트라이드로 자르며 각 윈도우를
  **끝점 샘플**(`per[end]`)로 라벨링하므로, L=128에서도 윈도우의 57%가 전환을
  가로지릅니다. PTB-XL(기록당 상수 진단)과 XJTU(스냅샷당 life 비율)는
  전환 비율 0입니다. 따라서 **HAPT는 "PTB-XL처럼 정적"이 아니라 독자적
  범주**이며, 적용성 표에 자체 행을 갖습니다.
- **HAPT의 활동 F1은 부분적으로 끝 구간 판독이지 느린 통합의 증거가 아닙니다**
  (2026-08-03 probe): (a) `z_slow` F1이 앵커 위치에 따라 단조 증가(시작 근처
  0.57 → 끝 0.71) — 문맥이 쌓이지만 causal 인코더가 "문맥이 많다"와 "라벨이
  붙은 끝점 샘플에 가깝다"를 혼동시킵니다. (b) 오염된 윈도우가 깨끗한
  윈도우만큼 또는 더 잘 나옵니다(0.78 대 0.74) — 상수 느린 인자 없이도 라벨이
  살아남습니다. 판정: F1의 **크기**는 윈도우 전체를 관통하는 느린 인자 통합의
  깨끗한 증거가 아니며, 깨끗한 부분집합의 F1(0.74)이 방어 가능한 수치입니다.
  블록 **분리** 주장은 별개이고 여전히 유효합니다.
- **분리는 설계상 한 방향입니다:** `z_mix`가 regime 정보를 갖는 것(≈0.57)은
  버그가 아니라 예상된 결과입니다.
- **Horizon 구성이 게이팅을 지배합니다 (2026-08-03 미해결 질문):** 작동하는 양은
  **τ 너머에서 학습된 horizon의 개수**입니다 — 그것만이 게이트를 닫고 `z_slow`에
  학습 신호를 줍니다(합성 5개 중 2개, HAPT는 auto-τ에서 6개 중 0개, 그래서
  HAPT가 작은 τ를 단조 선호). 비율 임계값(Δ_max/fast, Δ_max/dwell)은 사후
  관찰이지 설계 기준이 **아닙니다** — 비율을 가설로 제시하지 마십시오.
  Δ 집합 자체가 근거 없이 정해져 있습니다(합성 {1,4,16,64,128} 대 실데이터
  {1,2,4,8,16,32}, 기록된 정당화 없음) — 옛 하드코딩 τ와 같은 부류의 공백입니다.
  자세한 내용과 수치는 `docs/tau.md`.
- **스크린의 미묘한 점:** `screen()`이 현재 윈도우를 패치 평균(`W.mean(-1)`)으로
  뭉갭니다 — 암묵적인 8-샘플 저역통과입니다. 주기성 검정은 샘플 수준 신호에서
  돌려야 하며, 아니면 짧은 주기 교란이 보이지 않습니다.
- **체크포인트 재사용:** `runs/model_*.pt`와 `runs/emb_*.npz`가 있으며,
  shift 평가(P3-4)와 라벨 효율성(P3-5)은 재학습 없이 평가 층에서 돕니다.
- **하드웨어:** 전용 H200 1대. 전체 매트릭스는 야간 배치이며, **연산 기준의
  범위 축소는 무효**입니다 — 구속 조건은 구현 시간입니다.

## 6. 논문 프레이밍 상수 (모든 집필 작업에 적용)

- 논지 문장: horizon은 어떤 예측 학습기에도 이미 공짜로 있는 **지속성 감독
  축**이다 [예측 정보 계보: Bialek 2001, Creutzig 2009]. 표준 slow-fast mixing
  가정 아래 [cf. Pavliotis & Stuart] 지속성은 느림과 일치하며, 우리는 이 축을
  게이팅하면 느린 인자가 선형 부분공간으로 분리된다는 것을 **경험적으로**
  보인다. **학습에서는** 라벨이 필요 없다(검증은, 분리 표현 학습에서 표준이듯,
  라벨이 필요하다).
- "공짜"는 horizon 변수에만 적용되며 **분리 자체에는 절대 적용하지 않습니다**
  (d_slow와 λ는 라벨로 튜닝되고, HAPT는 −4pt의 다운스트림 비용을 보입니다).
- 8/8 수업 버전은 **"Responsible AI와의 관련성"** 소절(약 0.25쪽)을 추가로
  싣습니다: 설계에 의한 해석가능성(블록×인자 행렬로 측정) · 책임 있는 범위
  설정(스크린 + 예측된 음성 사례 + 정상데이터전용 제외) · 신뢰성(shift 결과) ·
  인식론적 정직성(Exclusion의 비보장, 선형 수준의 한계).
- 한계 서술 순서: 선형 수준의 배제 → 비지도 모델 선택 → xcov 극성 →
  두 시간척도 가정(스케일링은 마지막, 폭 스윕이 방어).
- 상시 메모: 분산 전용 regime 실험은 스크린/Method 산문에 정확히 한 문장의
  결과만 받습니다 — "분산 전용 regime에서 분리가 확인되는가와 두 스템이
  다른가". 평균 전용 이론 각주는 그 결과에 따라 부록으로 강등하거나 버립니다.
- **분산 전용 결과 (2026-08-03):** HGLP-Reg에서 분리 확인(regime 0.721±0.042) —
  평균 전용 조건은 이론적 경계이지 경험적 장벽이 아닙니다. 이론 각주는 부록으로.
  NCE-online도 여기서 실패하지만 표준 regime에서도 실패하므로 이것이 조건부
  평균 추정기에 대한 증거는 **아니라는 점을 명시**하십시오.
- **Related Works — horizon 집합 공백 (2026-08-03):** CPC [van den Oord 2018]은
  k=1..K의 연속 스텝을 k마다 별도 헤드로 예측하며 K는 도메인마다 손으로
  정하고 유도 과정이 없습니다. 우리는 로그 간격 부분집합을 단일 Δ-조건화
  예측기로 씁니다. 문헌에 **horizon 집합을 고르는 확립된 절차가 없으며**,
  그 공백이 스크린이 기여하는 바의 일부입니다(§5의 Δ 집합 미해결 질문과 연결).

## 7. 보고 프로토콜

작업일이 끝날 때마다 다음을 보고합니다. (1) 확보된 결과(run 태그),
(2) 논문이 이제 주장할 수 있게 된 문장, (3) ⛔에 걸렸거나 D1–D7에서 벗어난 것
(벗어남에는 사용자의 명시적 승인이 필요하며 **절대 자체 승인하지 않습니다**).
